<Page x:Class="StackOverflow_Statistics.Views.UsersMostReputationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StackOverflow_Statistics.Views"
        mc:Ignorable="d"
        Title="UsersMostReputationWindow" Height="450" Width="800"
        DataContext="{Binding Source={StaticResource Locator}, Path=UsersMostReputationViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="1"/>
        <!--TODO make a button on top left (reusable) to return and make filters)-->
        <Frame x:Name="usersMostReputation"/>
    </Grid>
</Page>
